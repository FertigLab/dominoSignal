# Normalize a matrix to its max value by row or column

Normalizes a matrix to its max value by row or column

## Usage

``` r
do_norm(mat, dir)
```

## Arguments

- mat:

  Matrix to be normalized

- dir:

  Direction to normalize the matrix (either "row" for row or "col" for
  column)

## Value

A normalized matrix in the direction specified.
