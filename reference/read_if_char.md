# Read in data if an object looks like path to it

Read in data if an object looks like path to it

## Usage

``` r
read_if_char(obj)
```

## Arguments

- obj:

  object to read if not already object

## Value

Object itself or data read in from path
